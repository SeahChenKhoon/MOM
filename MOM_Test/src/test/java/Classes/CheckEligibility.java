package Classes;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;

public class CheckEligibility extends Web_Driver {

/*	public void pageWait()
	{
		//Wait for Page to be ready
		wait.until(ExpectedConditions.visibilityOfElementLocated(By.xpath("//label[@class='control-label bgp-label']")));	
	}
	
	public void ClickNoForAllOptions()
	{
		List<WebElement> webElements = driver.findElements(By.xpath("//div[@class='controls bgp-radio-text-format']/label[2]/span[2]"));
		for (WebElement webElement: webElements) 
		{
			webElement.click();
		}
	}

	public void ClickYesForAllOptions()
	{
		List<WebElement> webElements = driver.findElements(By.xpath("//div[@class='form-horizontal bgp-radio-group']/div/div/div/label/parent::div/parent::div//parent::div/div[2]/label/span"));
		for (WebElement webElement: webElements)
		{
			webElement.click();
		}
	}

	public void Next_Click()
	{
		driver.findElement(By.xpath("//button[@id='next-btn']")).click();
	}

	public void Save_Click()
	{
		driver.findElement(By.xpath("//button[@id='save-btn']")).click();
	}*/
}
